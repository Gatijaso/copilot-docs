[metadata]
name = copilot-docs
version = 0.1.0
description = Utilities and demo for gold_model

[options]
package_dir =
    = src
packages = find:
install_requires =

[options.packages.find]
where = src
